## 📋 Запрос: GET Zero Users

### Назначение
Тестирование API при запросе пустого списка пользователей

### Технические детали
- **Метод:** `GET`
- **Коллекция:** Reqres
- **Окружение:** url, x-api-key
- **Теги:** `zero users`

### 📝 Пример запроса из файла
```http
GET /api/users/0 HTTP/1.1
Host: reqres.in
Authorization: API Key
```
### Успешный ответ (404 Not Found)
```json
{}
```
### 🧪 Проверки
```javascript
pm.test("Returns 404 for non-existent user", function () {
    pm.response.to.have.status(404);
});

pm.test("Response has correct structure for error", function () {
    const response = pm.response.json();
    // Проверка структуры ошибки
    pm.expect(response).to.be.an('object');
});
```
### 📊 Результаты тестирования
| Метрика | Значение | Статус |
|---------|----------|--------|
| Тест в RUN SUMMARY | Включен | ✅ |
| Особенность | Edge case тестирование | ✅ |
| Ожидаемый статус | 404 | ✅ |

### Важность теста
- Проверка устойчивости API к ситуациям на краях допустимых диапазонов
- Тестирование обработки граничных значений
- Валидация структуры ответа при отсутствии данных

### Нарушение REST-конвенций:
По стандартам REST, 404 ошибка должна содержать полезную информацию:
- RFC 7807 (Problem Details for HTTP APIs) рекомендует структурированные ошибки
- Даже минимально: {"error": "Not Found"}

